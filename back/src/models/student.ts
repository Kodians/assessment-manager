/**
 * @description find all the students
 * @returns Promise<object>
 */
export const findStudents = async (): Promise<unknown | void> => {}

/**
 * @description update a student
 * @param {number} studentId
 * @param {object} studentData
 * @returns Promise<object>
 */
export const updateStudent = async (studentId: number, studentData: unknown): Promise<unknown | void> => {}

/**
 * @description delete a student
 * @param {number} studentId
 * @returns Promise<object>
 */
export const removeStudent = async (studentId: number): Promise<unknown | void> => {}
